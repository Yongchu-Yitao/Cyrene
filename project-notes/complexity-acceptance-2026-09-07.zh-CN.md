# Cyrene 重构后的交互与性能验收

日期：2026-09-07。本轮在 macOS 本机执行，基线为 `30835bb6`，对照为 HEAD `e460975b` 加全部未提交工作区。工作区包含其他任务的更新、代理和缩放修改，不能把所有测量差异直接归因于复杂度重构。

## 结论

已补充真实 Electron 交互、真实 Chromium 缩放对照、浏览器 smoke 和顺序运行的性能基准。已执行的功能检查通过；**性能完全不变和跨平台验收尚不能标为通过**。没有发布、推送或合并。

## 真实界面与原生集成

使用独立 `/tmp/cyrene-acceptance-20260907/ui` 数据、缓存和临时目录；模型请求只发送到本机固定回复服务，不使用真实模型密钥。

| 检查 | 结果与证据 |
|---|---|
| 首次配置 | 自定义兼容端点、连接测试、默认人格、进入工作台成功 |
| 聊天发送 | 真实 HTTP、Agent 执行、流式展示和最终保存成功 |
| 草稿隔离 | 输入草稿后切换聊天，等待原聊天详情加载完成，再断言原草稿准确恢复 |
| 等待用户 | 固定模型发出真实 `ask_user` 工具调用；顶栏显示需要输入，详情显示选项；切换离开再返回后仍可回答 |
| 回答后继续 | 选择“继续验收”后运行完成，活动问题选项消失，顶栏变为已完成 |
| Guidance 与取消 | 运行中输入 guidance 并停止；后端 `runStatus=cancelled`、无 pending question，guidance 在已保存消息中恰好一次 |
| 取消后继续聊天 | 再次发送消息，运行正常完成并保存 |
| 代理设置 | 开启总开关和更新代理，刷新页面后更新代理仍开启，再恢复关闭；未实际通过代理下载更新 |
| 独立窗格 | 使用鼠标拖动主窗格到窗口外，创建真实独立窗口；调用原生归还 IPC 后窗口关闭，主窗口原草稿 `DETACH_DRAFT_验收` 保留 |
| 浏览器本地预览 | 本地 HTML、相对 JS/CSS、点击、存储、截图、网络隔离 smoke 通过 |
| 浏览器输入 | React input/textarea 和富文本输入持久化 smoke 通过 |
| 嵌套滚动 | 嵌套区域向下 240、向上后 120；根页面滚动保持 0 |

独立窗口的归还验证覆盖 renderer bridge、IPC、窗口回收与主窗口恢复，未自动化完整的原生鼠标归还手势。主聊天和 split composer 继续使用原有不同的草稿命名空间；没有把独立窗口初始空白误判为主草稿丢失。

截图：`output/playwright/cyrene-acceptance/returned-pane.png`，已实际打开检查。模拟模型刻意返回固定文本，不能据此评价真实模型能力、真实模型服务兼容性或外部网络速度。

## 性能方法和结果

使用同一 Python 虚拟环境、同一 Electron/原生辅助程序；基线源码单独解包，显式指定 PYTHONPATH。版本顺序运行，未在性能采样期间并行执行界面操作或另一版本基准。

### 聊天管线和本地功能

第一轮 before → after，每个场景 3 次；聊天再按 after → before，每个场景 5 次。以下为基准自身聚合的延迟指标，并非整机平均性能。

| 场景 | 第一轮基线 → 当前 | 变化 | 反向复测变化 |
|---|---:|---:|---:|
| 双会话重工具运行，p95 | 127.20 → 122.49 ms | -3.7% | +1.2% |
| 密集聊天并发，p95 | 519.66 → 569.20 ms | +9.5% | +5.5% |
| 多工具风暴，p95 | 947.31 → 979.80 ms | +3.4% | +6.9% |
| 事件广播 | 64.14 → 65.53 ms | +2.2% | — |
| 终端输出存储/回放 | 5568.53 → 5613.69 ms | +0.8% | — |
| 知识库 FTS 并发 | 2488.70 → 2481.63 ms | -0.3% | — |
| 知识库并发替换 | 794.70 → 793.42 ms | -0.2% | — |
| 数据库初始化 | 13.97 → 13.44 ms | -3.8% | — |
| 日程 CRUD 并发 | 1079.45 → 1154.33 ms | +6.9% | — |
| 文件哈希与缓存 | 6.15 → 7.06 ms | +14.9% | — |

全部场景的 `quality_preserved` 均为 true。密集聊天并发、工具风暴两轮均测得较慢，不能用默认 20% 告警阈值把它解释成“性能没有变化”。尚未完成差异归因。

代码核对发现 `chat_pipeline_benchmark.py`、`chat_runs.py`、`application/inbox.py` 相对基线未修改。该基准使用固定 runner 测量真实 Run Manager、流和 SQLite 管线，并不直接覆盖 AgentSession 的完整模型执行性能。现有文档所列独立 `agent` 组在实际入口中不存在，本轮没有声称运行该组。

### 桌面启动与内存

交替运行 3 组 before/after，每次使用全新隔离目录；计时范围从进程创建到完整 desktop smoke 退出，**不是首屏时间**。RSS 为每 50 ms 采样的进程树各进程 RSS 总和，包含共享页重复计数，**不是私有内存或泄漏量**。

| 指标（样本中位数） | 基线 | 当前 | 变化 |
|---|---:|---:|---:|
| 启动至 smoke 退出 | 5.251 s | 5.261 s | +0.2% |
| 进程树峰值 RSS 总和 | 1179.48 MiB | 1209.44 MiB | +2.5% |

6 次 smoke 全部通过设置 revision CAS 和快捷键同步。最初基线副本遗漏 Git 未跟踪的 macOS 原生辅助程序，补齐同一二进制后使用全新目录重测；该准备失败不计入上述样本。

### 长对话缩放

在真实 Electron/Chromium 中构建 1,000 条消息的固定 DOM，交替执行 6 组前后对照。此处“前”是本次 owner 提取前保存的 transcript 源码，保留了其他任务已有的缩放功能；CSS 为固定实验布局，不等同完整聊天页。

| 指标（中位数） | 提取前 | 提取后 |
|---|---:|---:|
| 冻结阶段 | 1.75 ms | 1.80 ms |
| 分帧恢复 | 259.25 ms | 260.80 ms |
| 几何读取次数 | 12,571 | 12,571 |
| 属性变更次数 | 3,956 | 3,956 |
| 冻结行数 | 989 | 989 |
| 恢复后冻结残留 | 0 | 0 |

每轮恢复回调恰好一次，`wbcResizeActive=false`。这些数据支持该 owner 提取保留布局读写和恢复策略，不能外推为所有渲染性能相同。

## 发现和修正

- 修正 `tests/electron_browser_type_smoke.js` 的旧 WebUI 资源路径。原来指向 `src/webui`，现在指向 `src/cyrene/workbench/webui`；输入行为断言保留，实际 smoke 已通过。
- 记录到 `/api/status` 返回 404。前端 `platform/data-store.jsx` 相对基线未改变，当前后端也没有该路由定义；它是现有接口不一致的线索，未作为本次重构回归修复。已走过的聊天流程仍完成，但不能称为控制台零错误。
- 记录到字体 preload 未及时使用的 Chromium 警告，未出现本轮交互操作引发的 React 异常堆栈。

## 最终检查与交付

最终本地 CI 检查全部通过：

- Python：**2,723 passed、2 skipped**，154.60 秒；将未处理线程异常设为错误。
- WebUI Node：**88 passed**；Electron Node：**125 passed**。
- Ruff、compileall、前端构建、Python/JavaScript 复杂度门禁、`git diff --check` 全部通过。
- 额外 3 个真实 Electron 浏览器 smoke 均以退出码 0 完成。

这关闭了上一轮全量 Python 中的测试星号导入失败；当前全量结果没有测试失败。

未检测到可用 Docker、PowerShell 或 QEMU 命令；没有在本机执行 Windows 安装/便携版和 Linux 实机 smoke。未触发远端 CI，未制作签名安装包。

建议提交边界已明确：复杂度门禁与基线、Core 所有者、后台投影与收尾、Electron 所有者、WebUI 状态/布局与视图、共享工作区功能整合、验收文档和测试路径修复。共享文件中仍混有其他任务功能修改，尚未整理成实际提交；性能验收也仍有待解释的差异，因此没有将当前树发布或合并。

可复查指标位于 `project-notes/complexity-acceptance-metrics-2026-09-07.json`；原始基准 JSON 和本轮脚本保存在 `output/performance/complexity-acceptance-20260907/`。这些脚本包含本轮本机绝对路径，重跑时应先更新源码/输出目录，并补齐基线依赖。

## 后续接口修复

上述 `/api/status` 404 已在后续根因修复中解决：恢复共享状态投影的 HTTP 查询入口，并独立出需鉴权的轻量 health 接口。实际 HTTP 与 CLI 发现已验证；详见 [接口修复记录](status-api-root-fix-2026-09-07.zh-CN.md)。此前性能测量仍保留原结论。

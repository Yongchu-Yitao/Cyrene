// The standalone PDF viewer is a separate browser surface. It still has one
// ESM entry and shares the same platform/PDF source modules as Workbench.
import "../platform/runtime.jsx"
import "../shared/pdf/bridge.jsx"

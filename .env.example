TELEGRAM_BOT_TOKEN=
OWNER_ID=
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-flash
ASSISTANT_NAME=Cyrene
# SCHEDULER_INTERVAL=60
# Optional additional roots for Workbench project directories.
# Separate multiple paths with the platform path separator (":" on macOS/Linux,
# ";" on Windows). Home, Cyrene's workspace, temp, and common mount roots are
# allowed by default.
# CYRENE_ALLOWED_WORKSPACE_ROOTS=/path/to/projects:/another/root
